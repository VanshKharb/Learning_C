#include <stdio.h>

int main(){
    /*  Constant: fixed value that connot be altered by the program during it's execution, to do this, we add "const" in front of
    data type and generally name is in uppercase   */

    const float PI = 3.14159; 

    printf("%.5f", PI);
}