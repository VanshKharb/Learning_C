#include <stdio.h>
#include <string.h>

int main() {

    char string1[] = "Vansh";
    char string2[] = "Kharb";

    /*strlwr(string1);                      // converts string to lowercase
    strupr(string1);                        // converts string to uppercase
    strcat(string1, string2);               // appends string2 to end of string1   
    strncat(string1, string2, 1);           // appends n characters from string2 to string1 
    strcpy(string1, string2);               // copy string2 to string1
    strncpy(string1, string2, 2);           // copy n characters of string2 to string 1 
    strset(string1, '?');                   // sets all characters of a string to a given character
    strnset(string1, 'x', 1);               // sets first n characters of a string to a given character
    strrev(string1);                        // reverses a string

    int result = strlen(string1);           // returns string length as integer 
    int result = strcmp(string1, string2);  // string compares all characters, if both the strings are equal then the output is 0, if the length of string1 is less than that of string2, then negative ans, and if string1 is greater than string2, then positive answer
    int result = strncmp(string1, string2, 2);  // string compares the first n characters
    int result = strcmpi(string1, string1); // string compares all characters (ignore case, for eg, hello and Hello will be equal)
    int result = strncmpi(string1, string1, 1); // compares the first n characters (ignore case) */

    // try to run them with using printf function
}