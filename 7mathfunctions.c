#include <stdio.h>
#include <math.h>              // in order to use math functions

int main() {

    double A = sqrt(9);        // for square root

    double B = pow(2, 4);      // 2 to the power of 4

    int C = round(3.14);       // rounding off

    int D = ceil(3.14);        //round off to the next integer

    int E = floor(3.99);       //Greatest integer function

    double F = fabs(-100);     // gives the positive value

    double G = log(3);         //logarithm function

    double H = sin(45);        // they take
    double I = cos(45);        // input in 
    double J = tan(45);        // radians

    printf("%lf\n", A);
    printf("%d", C); 

    return 0;
    
}